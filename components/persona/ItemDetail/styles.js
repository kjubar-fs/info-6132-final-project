import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        gap: 10,
    },

    itemNameContainer: {
        flexDirection: "row",
        alignItems: "center",
        gap: 6,
    },
});