import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        gap: 20,
        padding: 10,
        
        backgroundColor: "white",
    },

    affinityContainer: {
        flexDirection: "row",
        flexWrap: "wrap",
        gap: 10,
    },
});